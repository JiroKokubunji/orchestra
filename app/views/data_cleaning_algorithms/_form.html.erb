<%= form_with(model: data_cleaning_algorithm, local: true) do |form| %>
  <% if data_cleaning_algorithm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_cleaning_algorithm.errors.count, "error") %> prohibited this data_cleaning_algorithm from being saved:</h2>

      <ul>
      <% data_cleaning_algorithm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
